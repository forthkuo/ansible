- name: Ansible Copy Example Local to Remote
  hosts: all
  gather_facts: false
  tasks:
    - name: copying file with playbook
      copy:
        src: "{{source_file}}"
        dest: "{{dest_dir}}"
        owner: ans
        group: ans       
        mode: 0644
